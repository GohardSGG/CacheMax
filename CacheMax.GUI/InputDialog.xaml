<Window x:Class="CacheMax.GUI.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Prompt}" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBox Grid.Row="1" Name="InputTextBox" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10" VerticalAlignment="Center" Height="25"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="确定" Width="75" Height="25" Margin="0,0,10,0"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Name="CancelButton" Content="取消" Width="75" Height="25"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>